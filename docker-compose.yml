services:
  postgres:
    image: pgvector/pgvector:0.8.0-pg17-trixie
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - postgres:/var/lib/postgresql/data
    restart: always

volumes:
  postgres: {}